@model IEnumerable<PostHost.Content>
@{
    ViewBag.Title = "Home Page";
}

<div id="Gallery">
    @foreach (var m in Model)
    {
        <a href="@Url.Action("ViewSingle", "Home")">
            <img class="thumbnail" src="@Url.Content(m.ImgURL)" />
        </a>
    }
</div>